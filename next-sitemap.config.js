/** @type {import('next-sitemap').IConfig} */

const config = {
  siteUrl: 'https://www.scbhuey.com/',
  generateRobotsTxt: true, // (optional)
  changefreq: 'daily'
  // ...other options
}

module.exports = config